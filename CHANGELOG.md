# Changelog

## [2.0.0-eink] (branch eink-only)

### 剥离 Web

- 登录只走墨水屏扫码，不再要微信 Cookie / 官方 API Key。
- 下载只走 `chapterdownload`，失败不再回退 `e_0/e_1/e_3`。
- 账号菜单去掉微信登录。
- 书架 `/shelf/sync`、进度 `/book/getProgress`、搜索 `/store/search`、目录 `chapterinfo` 走 eink。
- 划线走 `bookmarklist` + `bestbookmarks`，不再打 Skill gateway。
- 想法 `/book/readreviews`、阅读上报 `/book/read` 走 eink POST。


## [1.2.41]

### TXT 排版

- 转 XHTML 时去掉段首全角/半角空格，缩进交给 epub.css。已下好的书不受影响。


## [1.2.40]

### 进度同步

- 合订 EPUB 改名后 sidecar 仍是旧路径，进度同步当成单章且找不到当前章（`current_chapter_not_found`）。按同一目录去括号文件名识别为全书。


## [1.2.39]

### 菜单

- 「下载当前章节的想法」挪进「划线与想法管理」，不再和外层并列。


## [1.2.38]

### 开书闪退与重命名

- 标注层 `getVisiblePageCount` 漏传 document，改名后当本地书匹配会把 KOReader 打崩。已修。
- 同一目录下只改文件名（去掉书名括号）时，自动对回原来的微信读书记录。


## [1.2.37]

### 菜单

- 「下载当前章节的想法」只留阅读菜单外层，管理子菜单里那条重复入口去掉。


## [1.2.36]

### 下载当前章节想法

- 阅读菜单增加「下载当前章节的想法」，只拉正在看的那一章，不再对合订本整本穷举。


## [1.2.35]

### 想法下载不再卡死合订 EPUB

- 点一条划线只修当前章，不再对 2562 章合订本整本重拉想法。
- 大书（超过 80 页）禁止 `findAllText` 全书扫描；匹配不到就跳过。
- 长书同步时不再每章刷新标注层。
- 想法/同步进度打 INFO 日志，避免再卡死时 crash.log 空白。


## [1.2.34]

### 撤回错接的划线接口

- 1.2.33 把想法修复 / 本地书匹配的 `/book/underlines`（公开热力图）换成墨水屏 `bookmarklist`（你自己的划线）。两套数据不一样；空列表还会挡住 web 回退。已撤回。
- 汉化保留。


## [1.2.33]

### 划线与想法汉化

- 补齐菜单中遗漏的划线与想法汉化（本地书匹配、划线管理、设置项确认弹窗等 36 条）。


## [1.2.32]

### 账号菜单

- 「立即续期 Cookie」只服务微信登录，挪进微信登录子菜单，不再和墨水屏平级。


## [1.2.31]

### 账号菜单

- 首页不再并排两个扫码。只留「账号」，点进账号管理。
- 微信登录（书架/进度）和墨水屏登录（整本快下）分开。已登录可重新扫码或单独退出。
- 不再把 Cookie/API/目录糊成一份账号状态。


## [1.2.30]

### 大书组装

- TXT/eink 没有脚注扫描数据时跳过整段脚注（1.2.29 上 2562 章空扫占了 6 分 46 秒）。
- 组装批次 20→80。现在按章从硬盘读，内存不再是限制。
- 检查点每 200 章存一次并打日志，避免后期每批写整份 JSON。
- 进度日志不再出现 `2563/2562`。


## [1.2.29]

### 墨水屏改落盘

- `chapterdownload` 不再整包进 Lua 字符串。先写到工作目录文件，tar 再解到磁盘，按章读取。
- 和 VPS 测 ZIP 落地同一思路：档案在硬盘上，不在内存里堆 2562 章。


## [1.2.28]

### 墨水屏大书卡死

- 1.2.27 对上 2562 章后，一次性把全部章节转 xhtml、打划线、写检查点，Kindle 主线程卡死。
- 改为每次只转写 20 章并让出 UI；划线不走这条快路径。
- 转完一章就丢掉源文件，检查点按批保存。


## [1.2.27]

### 墨水屏 TXT TinyFile 映射

- 真机日志：`files=2563 mapped=0`，样本名 `465030_1330_o`。
- 按 `{bookId}_{chapterUid}_o` 映射章节；纯文本包 xhtml。
- 优先读 tar 内 `info.txt` 的 files 列表。
- mapped=0 时日志带 payload 类型（text/html/gzip/binary），不再只打文件名。


## [1.2.26]

### 墨水屏 tar 章节映射

- 1.2.25 能解开 tar 并打 `eink zip download ready`，但只认 `.xhtml/.html`。TXT 网文（如 2000+ 章）映射为 0，随后静默走 web 逐章。
- 按 `chapter.files` / 章节 uid / 文件名 basename 匹配 `.txt`、无后缀和 xhtml。
- 纯文本自动包一层 xhtml。
- tar 认历史 typeflag 空格和 ustar prefix。
- 日志增加 `files=` `mapped=`；mapped=0 时打出样本文件名。


## [1.2.25]

### 墨水屏 ZIP/tar

- `chapterdownload` HTTP 200 实际会返回 tar（`info.txt` 头），原先只认 ZIP `PK`，被误判失败后回退 web。
- 识别到 tar 时直接解包，不再走 ZipCrypto。


## [1.2.24]

### 撤回 ZIP 分批

- 撤回 1.2.22 按 80 章分批 `chapterdownload`。一次仍拉整本 ZIP。
- 登录隔离（1.2.23）保留。


## [1.2.23]

### 登录态隔离

- 墨水屏扫码只写 `settings.eink`，不再整表覆盖 `account`，Web Cookie / `login_method` 不会被挤掉。
- Web 扫码只更新 cookie 和 account，不碰 eink 凭证。
- `account` 改为字段合并，不再整份替换。
- ZIP 下载只使用墨水屏 `vid` + `accessToken`，不再拿 `wr_skey` 冒充。
- 主菜单 Web「已登录」看 cookie 是否存在，不看被墨水屏改过的 `login_method`。


## [1.2.22]

### 墨水屏 ZIP 分批下载

- `chapterdownload` 按每批 80 章拉取，避免 2000+ 章一次请求返回 JSON 而不是 ZIP。
- ZIP 失败时日志带上 HTTP 状态和 errcode/errmsg，不再只写 “did not return a ZIP”。
- 部分批次成功时保留已解开章节，剩余章节再走 web worker。


## [1.2.21]

### 墨水屏登录状态

- 「墨水屏已登录」只认本地 `eink.vid` / `access_token`，不再把 Web Cookie 的 `wr_vid` 显示成已登录。
- 没单独扫墨水屏码时，入口仍是「墨水屏扫码登录」。Web cookie 照样能走 ZIP。


## [1.2.20]

### 墨水屏扫码登录入口

- 主菜单和「账号管理」增加墨水屏扫码登录，和现有 Web 扫码并存。
- 墨水屏凭证写入 `eink`（vid / accessToken），不覆盖 Web Cookie。
- 账号状态展示 Eink 是否已配置。

### 验证

- Lua spec 需全绿。
- luacheck：0 warnings / 0 errors。


## [1.2.19]

### 墨水屏 ZIP 下载（兼容旧 Web 通道）

- 新增 eink 原生下载：一次 `chapterdownload` 拉取加密 ZIP，用响应头 `encryptKey` + vid AES 解密后组装 EPUB。
- 现有 Web Cookie / Skill API Key / `e_0` 碎片下载全部保留；eink 失败自动回退 web worker。
- 已有 `wr_vid` + `wr_skey` 即可走快路径，不必强制第二套扫码。
- 划线优先一次拉取 `/book/bookmarklist`，再按章回退 gateway。
- 整本下载 worker 路径会先尝试 ZIP 写入检查点，未命中章节再走 web worker。

### 验证

- Lua spec 需全绿。
- luacheck：0 warnings / 0 errors。

## [1.2.18]

### 下载暂停与继续

- 下载对话框新增“暂停下载 / 继续下载”按钮。
- 暂停时终止活动 worker、停止后续网络请求，保留已完成章节、资源和检查点。
- 继续时从检查点恢复，跳过已完成章节；“取消下载”仍保持原有清理语义。

## [1.2.17]

### Reader 状态修复

- 修复多章下载 worker 复用首章 Reader 状态，导致后续章节收到 HTTP 200 + `{}` 并批量失败的问题。
- 每个章节 worker 现在刷新对应章节的 Reader 状态；已完成章节继续通过 manifest 断点恢复。
- 单章连续失败达到重试上限后立即停止整本任务并提示，不再把剩余两千多章全部跑完；已完成章节和检查点保留。

## [1.2.16]

### 下载卡死修复

- 修复章节 worker 子进程结束但未写回 pipe 时，任务永久停在 0% 的问题；现在会正确回收并进入重试。
- 多章下载启动后显示当前章节和总进度，避免进度对话框看起来没有响应。

## [1.2.15]

### 大书下载优化

- 整本下载复用 Reader 状态，减少每章重复初始化和请求开销。
- 章节正文、资源索引和脚注扫描结果逐章落盘，支持强制重启后的断点续传。
- `e_0/e_1/e_3` 正文分片采用受控并发下载；整本下载支持最多 4 个章节 worker，可在“设置 → 下载设置 → 章节并发数”中选择 1–4，默认 2。
- 最终 EPUB 在子进程中构建，主界面轮询构建状态，降低大书组包时的界面假死和内存压力。
- 构建失败或进程异常时保留检查点，避免已下载章节全部作废；成功后才清理临时工作区。
- 修复静态检查告警，确保发布包通过完整 CI 校验。

## [1.2.14]

### 大书下载优化

- 整本下载复用 Reader 状态，减少每章重复初始化和请求开销。
- 章节正文、资源索引和脚注扫描结果逐章落盘，支持强制重启后的断点续传。
- `e_0/e_1/e_3` 正文分片采用受控并发下载；整本下载支持最多 4 个章节 worker，可在“设置 → 下载设置 → 章节并发数”中选择 1–4，默认 2。
- 最终 EPUB 在子进程中构建，主界面轮询构建状态，降低大书组包时的界面假死和内存压力。
- 构建失败或进程异常时保留检查点，避免已下载章节全部作废；成功后才清理临时工作区。

## [1.2.13]

### 移植上游 v1.4.2

- 划线/想法请求批次从每批 5 条提升到 30 条，减少网络请求次数。
- 修复注释同步进度条，显示当前章节内的想法下载和划线匹配进度。
- 修复注释清理范围，整本书的旧 projection、缓存状态和跨文件坐标都会正确清除。
- 文档 key 不再受文件 ctime 抖动影响，重复打开同一 EPUB 不会无故触发重新匹配。
- 有序注释 projection 使用区间索引，减少翻页时的全量 XPointer 投影。
- 打包时自动清理 `.DS_Store` 文件。
- 保留二开版的扁平 EPUB、独立 metadata、公众号 sidecar、自研更新器及账号/预取安全修复。

### 验证

- 64 个 Lua spec 全部通过。
- luacheck：0 warnings / 0 errors。
- Lua namespace、语法和 Release 打包检查通过。
- 本次变更尚未推送，待远程 CI 验证后发布。

## [1.2.12]

### 稳定性与更新器修复

- 账号切换时终止旧进度同步子进程，防止旧账号的凭据或阅读进度写入新账号。
- 账号切换同步取消正文预取和划线/想法预取。
- 注释预取在网络响应返回后再次检查取消状态，避免清理后写回旧数据。
- 章节预取生成并保存统一注释 descriptor，打开预取章节后可正确恢复注释显示。
- 更新器限制下载地址、增加更新包大小上限和 Release SHA-256 校验。
- 启动时自动恢复更新中断留下的插件备份目录。

### 验证

- 63 个 Lua spec 全部通过，新增账号切换与取消竞态回归覆盖。
- luacheck：0 warnings / 0 errors。
- KOReader PluginLoader 集成检查通过。
- Release 包完整性检查通过。

## [1.2.11]

### 上游 v1.4.0 适配

- 移植脚注识别修复：避免章节根节点污染脚注正文，同时保留日文、韩文、俄文和扩展汉字脚注。
- 清理服务器书籍 CSS 中会导致正文塌缩的根节点 `font-size: 0`，不改动其他选择器的有意设置。
- 公众号文章图片改为流式落盘，去除 base64 内存副本；重复图片复用同一文件，失败时保留远程地址。
- 统一本地书与微信下载书的划线/想法匹配，按章节边界匹配，支持断点续传、取消后恢复和跨文件复用。
- 下一章正文预取改为后台子进程，注释数据单独预取；增加低内存、超时、取消和认证回写保护。
- 新增“预取划线和想法”独立开关，默认关闭；保留原有扁平 EPUB、独立 metadata、公众号 sidecar 和自研更新器。

### 验证

- 63 个 Lua spec 全部通过。
- luacheck：0 warnings / 0 errors（139 个文件）。
- Lua namespace、语法检查通过。
- 性能诊断日志保持默认关闭。

## [1.2.10]

### 界面调整

- 隐藏自适应封面书架中每本封面图片外侧的黑色边框，保留封面点击区域和已下载折角标记。

## [1.2.9]

### 上游适配

- 适配上游 v1.3.1：整本下载的 EPUB 现在会写入书籍简介，可在 KOReader 的书籍信息中查看。
- 优化自适应封面书架：封面格按可用区域铺满，移除封面下方状态文字，修复部分屏幕上的内容溢出和滚动条问题。
- 为已下载书籍增加右上角折角标记，避免依赖额外图片资源。

## [1.2.8]

### 问题修复

- 修复想法数据中的异常 `pageReviews` 条目导致整章想法数据库写入中断的问题；异常条目现在会被跳过，合法想法继续保存。
- 清理旧版本 EPUB 中已经写入的划线末尾星号，同时保留正文中的普通星号。
- 修复隐藏正文脚注时，部分使用 `aside`、`li`、`div` 或 `section` 容器的脚注正文未被移除的问题。
- 脚注显示设置写入下载生成的 EPUB；已经存在的旧 EPUB 需要重新下载后生效。

## [1.2.7]

### 新功能与改进

- 新增自适应封面书架（设置 → 书架视图 → 封面模式），并默认启用分页，提升大书架加载速度。
- 重做想法弹窗，支持 Emoji、长内容滚动、物理按键、显示样式调整，以及长按复制想法。
- 下载设置新增脚注弹窗选项，可隐藏正文中的脚注文本；默认仍为页内脚注。
- 移除划线末尾星号，避免影响正文排版。
- 修复阅读进度自动同步偶尔失败、封面书架显示不稳定等问题。

### 本仓库保留项

- 扁平 EPUB、独立 metadata、公众号文章 sidecar。
- 自研在线更新器与国内代理通道。
- 本地书外部划线/想法覆盖层，以及可开关的性能埋点。

## [1.2.6]

### 诊断修复

- 修正性能埋点的 KOReader 定点时间单位，日志中的 `ms` 现在是真实毫秒值。
- 保留 `advanced.developer_logs` 开关，默认关闭。
- 修正诊断计时相关纯 Lua 测试环境的可选依赖处理。

## [1.2.5]

### 诊断与测试

- 新增“开发者诊断 → 性能计时日志”开关，默认关闭，不影响日常日志和性能。
- 开启后记录开书、关书、路径索引、单书持久化、上传回写和想法数据库打开耗时。
- 埋点统一使用 `[WeRead][Perf]` 标记，便于从 KOReader 日志中筛选。
- 新增性能埋点开关的回归测试。

## [1.2.4]

### 性能优化

- 新增 `Settings:update_book()` 单书持久化接口，进度和阅读时长上下文更新不再重写整个书库。
- 关闭书籍时只保存当前书的进度，减少 Kindle 闪存上的同步写入和等待。
- 阅读时长上报的上下文回写改为单书更新，同时保留旧版 settings 对象的兼容 fallback。
- 新增 Settings、ProgressSync、ReadReport 单书持久化回归测试。

## [1.2.3]

### 性能优化

- 扁平 EPUB 现在优先通过轻量级原始索引识别，避免开书时加载整个书库的 BookStore 数据。
- 缓存当前文档的文件路径和 book_id，同一文档后续的翻页、同步和关闭流程不再重复扫描书库。
- 保留旧版嵌套书籍目录的兼容识别逻辑。
- 新增原始路径索引和当前文档缓存的回归测试。

## [1.2.2]

### 优化与问题修复

- 打开微信读书书籍时不再立即初始化本地想法数据库，改为首次点击划线时懒加载，减少开书等待。
- 移除每次打开想法数据库时重复删除旧表的操作，避免不必要的 SQLite 写操作。
- 新增针对开书生命周期和想法数据库 schema 的回归测试。

## [1.2.1]

### 优化与问题修复

- 锁屏时仅在本地保存待上传进度，不再从 suspend 回调发起网络请求。
- Kindle 唤醒后等待 `NetworkConnected` 再静默补传进度并校验云端位置，避免 Wi-Fi 恢复期间弹出离线提示。
- 自动进度拉取和补传改由子进程执行，网络延迟不再阻塞 KOReader 界面。
- 锁屏时不再同步终止正在上报阅读时长的子进程，避免进程回收拖慢锁屏。

## [1.2.0]

### 新功能

- 本地书支持同步微信读书的划线与想法，无需重新生成 EPUB；可取消、断点续传，并可一键显示或隐藏划线。
- 新增可绑定到手势或按键的划线想法显隐动作。

### 优化与问题修复

- 整本下载失败时自动重试章节请求，不再保存残缺 EPUB。
- 云端转换文档的资源包兼容 ZIP，避免按 TAR 误解析导致下载失败。
- 阅读器菜单按当前文档类型显示：微信读书缓存书显示同步进度/书籍详情，本地书显示划线想法同步入口。

本地书划线与想法功能参考了 https://github.com/Mr54233/pickthought.koplugin 的实现。感谢 @lostanother、@Mr54233 的贡献。

### 本仓库保留项

- 扁平 EPUB 书库与独立元数据目录。
- 自研插件更新器、国内 GitHub 代理与 push 触发发版流程。
- 独立划线和想法确认开关，以及公众号排版与 sidecar 优化。
- 不吸收上游本地收藏夹 / weread收藏夹。

## [1.1.0]

### 新功能

- 支持使用方向键、翻页键和确认键操作书架、书籍详情、章节列表、书评与阅读统计。
- 新增可绑定到手势或按键的书架、搜索和阅读统计动作，并保留本仓库原有的阅读进度同步动作与阅读界面快捷菜单。
- 新增书评评论接口，为后续评论界面提供数据能力。

### 优化与问题修复

- 图片及 EPUB 资源改为磁盘流式处理，降低大书下载时的内存占用，并自动清理下载临时文件。
- 脚注跟随上游改为 KOReader 默认页内显示，避免默认设置下脚注不可见或点击后跳错位置。
- 修复想法星标在划线末尾丢失或被挤到下一行的问题。
- 修复阅读上报开关状态不及时刷新的问题。
- 修复空书架、空书评、快速切换书籍，以及书架残留阻挡 KOReader 退出的问题。

> **升级提示：**脚注显示方式会写入下载的 EPUB；已经下载的旧书需要重新下载才能使用页内脚注。

### 本仓库保留项

- 扁平 EPUB 书库与独立元数据目录。
- 自研插件更新器、国内 GitHub 代理与 push 触发发版流程。
- 独立划线和想法确认开关，以及公众号排版与 sidecar 优化。
